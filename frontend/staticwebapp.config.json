{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/*.{png,jpg,gif,svg,ico,json,js,css}"]
  },
  "routes": [
    {
      "route": "/auth/callback",
      "rewrite": "/index.html"
    }
  ]
}